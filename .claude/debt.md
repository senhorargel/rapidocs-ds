# Dívidas técnicas

Formato: `[ ] item — motivo — desde quando` (marcar [x] quando resolvido)

## Fundação (tokens)

- [x] Tokens em `tokens/src/` são placeholders — ~~substituir pela extração real do Figma via MCP~~ **RESOLVIDO (cores) em 2026-07-21**: 219 primitivas + 61 semânticas extraídas do Figma — 2026-07-15
- [x] ~~Extrair as demais variables do Figma~~ **RESOLVIDO em 2026-07-21**: `escala.json` (padding/gap/component), `forma.json` (raio, modo Default), `tipografia.json` (família + pesos + tamanhos) e `text-styles.json` (28 estilos). Só a camada `font-family/Font-Size/*` ficou de fora (cross-library, ver item abaixo) — 2026-07-21
- [x] ~~`color/yellow/yellow/...` com "yellow" duplicado~~ **CORRIGIDO no Figma (20 variáveis renomeadas p/ `color/yellow/...`) e re-sincronizado** — 2026-07-21
- [x] ~~`color/alpha/Sucess/...` grafado "Sucess"~~ **CORRIGIDO no Figma (7 vars → "Success") e re-sincronizado** — 2026-07-21
- [x] ~~Saída Dart duplicada em `tokens/build/dart/`~~ **RESOLVIDO em 2026-07-26**: os tokens Dart passaram a ser gerados dentro do pacote Flutter (`components/flutter/lib/src/tokens/`), a pasta antiga foi apagada, o build apaga se ela reaparecer (`DART_BUILD_PATH_ANTIGO` em `tokens/build-tokens.mjs`) e os `exports` `./dart` / `./dart-dark` saíram do `tokens/package.json` — 2026-07-21 · ⚠️ **a exclusão ainda não está commitada** — ver item aberto logo abaixo
- [ ] **A exclusão de `tokens/build/dart/` só existe na área de trabalho**: `git ls-tree -r HEAD --name-only` ainda lista `tokens/build/dart/tokens.dart` e `tokens.dark.dart` (o `git status` mostra os dois como ` D`, deletados e **não** preparados). Um `git checkout .` devolveria a cópia antiga — que é a versão **quebrada**: classe `RapidocsTokens` com descrições em comentário de bloco (`/** */`, 62 deles, que aninham em Dart e engolem o arquivo) e as dimensões com o fator 16x (`componentS = 192.00`, `component20 = 320.00`). Precisa entrar no commit da `feature/componente-alert` — 2026-07-26
- [x] ~~Tokens Dart não compilavam~~ **RESOLVIDO em 2026-07-26**: a descrição do token saía como comentário de bloco e, como comentário de bloco **aninha** em Dart, engolia 437 das 438 declarações. Agora sai como doc comment de linha (`///`) pelo formato próprio `flutter/class-rapidocs.dart`, e a função `travaDart()` do build FALHA se a profundidade de comentário ou as chaves não fecharem — 2026-07-26
- [x] ~~Dimensões Dart 16x maiores~~ **RESOLVIDO em 2026-07-26**: o `transformGroup: 'flutter'` do Style Dictionary aplica `size/flutter/remToDouble` (assume rem) e multiplicava tudo por 16. A plataforma `dart` passou a usar lista explícita de transforms; conferido na saída atual (`componentS = 12.0`, `component20 = 20.0`, `paddingS = 12.0`) — 2026-07-26
- [ ] **Hover quase invisível no tema escuro**: `action/hover/onColorSecondary` vale `rgba(8, 9, 10, 0.04)` (preto a 4%) no claro **e** no escuro — `semanticos.dark.json` até redeclara o token, mas apontando para a mesma primitiva `{color.alpha.dark.4}` do tema claro (conferido em `tokens/build/web/tokens.css:353` e `tokens.dark.css:29`). Um véu preto a 4% sobre superfície escura praticamente não aparece: o hover do X e do link some no modo escuro, nas duas plataformas. Dívida de **FUNDAÇÃO**, não do Alert: o Figma precisa definir um valor de hover próprio para o modo escuro (por exemplo, um alpha claro) e re-sincronizar — 2026-07-26
- [ ] **Contraste do ícone sobre o próprio fundo abaixo de 3:1** — medido na saída atual (tema claro): warning **1,81:1** · success **2,22:1** · info **3,26:1** · error **3,92:1**. Vem da fonte: o Figma pinta o ícone com `feedback/(status)/fill` sobre `feedback/(status)/surface`. É dívida de **FUNDAÇÃO**, não do Alert — avaliar um tom mais escuro do `fill` ou um token dedicado ao ícone sobre surface. Só info e error passam hoje — 2026-07-26
- [ ] **`radii/surface` não existe em `tokens/src/forma.json`** (conferido: a palavra `radii` não aparece em nenhum arquivo de `tokens/`) e **não** é o mesmo que `component/s`: no Figma o Alert usa `radii/surface` no **Large** e `component/s` no **Small**; os dois coincidem no modo Default (12) e no Sharp (2), mas divergem no Rounded (`component/s` = 16 vs `radii/surface` = 32). Os modos `Rounded`/`Sharp` da coleção `shape` também não estão no build — `forma.json` só tem os 9 raios do modo `Default`. Precisa ser extraído do Figma; enquanto isso o código usa `component/s` nos DOIS tamanhos e nas duas plataformas (`--component-s` em `components/angular/alert/alert.css`, `RapidocsTokens.componentS` em `components/flutter/lib/src/alert/rds_alert.dart`) — 2026-07-21
- [x] ~~O build imprimia `Token collisions detected (1)` a cada execução~~ **RESOLVIDO em 2026-07-26**: a colisão não era dos tokens `component/*` (os sufixos diferem: numérico em `escala.json`, t-shirt em `forma.json`) — era o `$type` declarado na **raiz** dos arquivos-fonte, que o Style Dictionary funde como se fosse uma chave e via colidir entre `semanticos.json`, `escala.json` e `forma.json`. O `$type` saiu da raiz e passou a ser declarado por grupo (e, nesses dois arquivos, em **cada token**, porque os grupos `component` de primeiro nível se fundem). O aviso não aparece mais; se voltar, é colisão de verdade e vale investigar — explicação completa em `tokens/build-tokens.mjs`. Segue valendo o cuidado: não repor `$type` na raiz nem no grupo `component` desses dois arquivos — 2026-07-21 · resolvida 2026-07-26
- [~] 9 variáveis `font-family/Font-Size/*` (Caption, Label, Title Mobile) referenciam outra biblioteca do Figma (cross-library) — **contornado**: tipografia modelada via primitivas locais (`.primitive-fonts`) + os 28 text styles (valores concretos); a camada semântica `Font-Size/*` foi pulada. Reavaliar se essa biblioteca externa for integrada — 2026-07-21
- [ ] **Text styles no Dart não existem**: só o CSS (`tokens.typography.css`, classes `.text-*`) é gerado. Falta emitir `TextStyle`s Dart — por isso o `RdsAlert` traz tamanho (14/12), peso (400/600/700) e altura de linha (1,45) como literais no componente — 2026-07-21
- [ ] `RapidocsCores` (resolução claro/escuro no Flutter) é escrito à mão, 62 cores semânticas uma por uma — cor nova no Figma exige adicionar o getter na mão. Gerar esse arquivo junto com os tokens resolveria — 2026-07-26
- [ ] Scripts raiz `build:tokens`/`storybook`/`build:storybook` chamam `pnpm` direto (sem corepack) — falham localmente se pnpm não está no PATH (no CI funciona). Local: usar `corepack pnpm --dir <pacote> run <script>` — 2026-07-21

## Componentes (Alert / Fase 2)

- [ ] **Quirks de nome na fonte do Figma**: a opção de status está grafada **`Uptade`** (deveria ser `Update`) e a propriedade de conteúdo, **`Edite Tittle`**. O código já usa a grafia certa (`update`), mas a fonte continua errada — corrigir no Figma e re-sincronizar — 2026-07-26
- [ ] **Ícone do status `update` é o "placeholder" do Figma** — a fonte ainda não definiu um desenho próprio para esse status. Trocar `assets/icons/alert/update.svg` (e a cópia do Flutter) quando o ícone definitivo existir — 2026-07-26
- [ ] **SVGs dos ícones duplicados**: `assets/icons/alert/` (raiz) e `components/flutter/assets/icons/alert/` são o mesmo conteúdo, sincronizado **à mão** — um pacote Dart só empacota assets que estão dentro dele. Se alguém trocar o ícone na raiz e esquecer da cópia, web e mobile divergem. Hoje (2026-07-26) os 6 SVGs estão idênticos nas duas pastas — conferido byte a byte. Automatizar (passo no `build:tokens` ou no CI) — 2026-07-26
- [ ] **Prévia "App (Flutter)" da vitrine é simulação**, não o widget Flutter rodando: é HTML com os mesmos tokens do Flutter (o Storybook roda no navegador). Cada story avisa isso na tela. Embutir Flutter web de verdade é possível, mas exige SDK instalado + passo de CI — 2026-07-26
- [ ] **Dart/Flutter não instalados** na máquina onde o DS é mantido: `dart analyze`, `flutter test` e build não rodam nem local nem no CI (o único workflow é `.github/workflows/deploy-storybook.yml`). Existem **11 testes de widget escritos** em `components/flutter/test/rds_alert_test.dart` (status × tamanho, fundo por token, corpo/link vazios, X inerte sem `onDismiss`, X com callback, link sem consumidor anunciado desativado e fora do foco, sublinhado do link no hover e no foco, anel de foco em volta do ícone, anel de foco em `content/primary`, link sem fundo em nenhum estado, hover no alvo de 44/40 acendendo a caixa do ícone) — mas **nunca foram executados**. O código Dart foi revisado à mão e pela trava de sintaxe do build de tokens. Rodar no primeiro ambiente com SDK é pré-requisito para fechar a Fase 2 — 2026-07-15
- [ ] **Nenhum teste automatizado do lado Angular**: `components/angular/` não tem `*.spec.ts` nem runner configurado, enquanto o Flutter já tem 11 testes de widget. As regras de paridade (link/corpo vazios, X desativado sem consumidor, `preventDefault` no href padrão) só estão cobertas por teste em uma das duas plataformas — 2026-07-26
- [x] ~~A vitrine não seguia as regras de paridade de conteúdo~~ **RESOLVIDO em 2026-07-26**: em `apps/storybook/stories/components/Alert.stories.ts`, `renderAlert()` agora confere o texto antes de mostrar (`isLarge && showBody && body` para o corpo, `showLink && link` para o link — mesma regra do Angular e do Flutter), e o X dos exemplos tem consumidor de verdade: todo exemplo passa por `ligarFechar()`, que remove o alerta da página e oferece o botão "Restaurar exemplo" (deixando claro que quem remove é a aplicação, não o componente) — 2026-07-26
- [ ] **Alvo de toque do X em 44px (large) / 40px (small) nas DUAS plataformas** — abaixo dos 48×48 que o Material recomenda. É **desvio consciente**, decidido em 2026-07-26: o alvo é o ícone (20/16) mais os 12px de `padding/s` de cada lado, ou seja o maior possível sem empurrar o layout do Figma; subir para 48 exigiria aumentar o padding do container, o que é **decisão de design**. Passa dos 24×24 da WCAG 2.5.8. O DESENHO do ícone continua 20/16, como na fonte. Onde está: `alert.css` (`.rds-alert__close::before { inset: calc(-1 * var(--padding-s)) }`) e `rds_alert.dart` (`alvo = tamanhoIcone + RapidocsTokens.paddingS * 2`) — 2026-07-26
- [ ] Publicação em npm (`@rapidocs/ds-angular`) e pub.dev (`rapidocs_ds`) — hoje o consumo é via Git; publicar é Fase 2 — 2026-07-15
- [ ] **Não há tags de versão publicadas** no repositório (`git tag` volta vazio), mas
  `components/flutter/README.md` e o antigo texto de instalação sugeriam `ref: v0.1.0` como
  alternativa ao `ref: main`. A página **Fundação → Instalação** já documenta só o que
  funciona hoje (`ref: main`), com o aviso. Criar a primeira tag (ex. `v0.1.0`) para o dev
  poder fixar versão em vez de seguir a `main` — 2026-07-26
- [ ] **`@rapidocs/ds-angular` publica TypeScript cru** (`public-api.ts` sem build): quem
  consome via Git precisa de um fallback de `paths` no `tsconfig.json`. Está documentado na
  página de Instalação, mas o certo é a biblioteca ter build próprio (ng-packagr) e publicar
  os artefatos — some a pegadinha para quem instala — 2026-07-26
