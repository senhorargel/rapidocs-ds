# Componentes Flutter — Rapidocs DS

**Fase 2.** Componentes em Dart/Flutter, construídos sobre os tokens gerados
em `tokens/build/dart/tokens.dart` (classe `RapidocsTokens`).

Nada aqui ainda — a fundação (tokens) vem primeiro. O Flutter/Dart só precisa
ser instalado quando esta fase começar.
