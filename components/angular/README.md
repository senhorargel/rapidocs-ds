# Componentes Angular — Rapidocs DS

**Fase 2.** Componentes em TypeScript/Angular, construídos sobre os tokens de
`@rapidocs/tokens` (`build/web/tokens.css`).

Nada aqui ainda — a fundação (tokens) vem primeiro.
