# Em monorepo, prefira o pacote local do workspace quando o nome/versao bater.
# Sem isso, o pnpm tenta baixar @rapidocs/tokens do registro npm (onde nao existe,
# 404) ao resolver os peerDependencies de components/angular.
link-workspace-packages=true
